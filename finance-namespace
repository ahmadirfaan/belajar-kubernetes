apiVersion: v1
kind: metadata
metadata:
  name: finance






# CARA MEMBUAT NAMESPACE
# COMMAND untuk membuat namespace dengan kubectl "kubectl create -f <namefile> --namespace finance
# COMMAND untuk menghapuse namespace "kubectl delete namespace <nama-name-space>"